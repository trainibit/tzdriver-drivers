/*
package com.trainibit.tzdriver_drivers.mapper;

import com.trainibit.tzdriver_drivers.entity.State;
import com.trainibit.tzdriver_drivers.response.StateResponse;
import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface DriverMapper {
    */
/*@Mappings({
            @Mapping(source = "name", target = "name"), //en sourge especifico la propiedad dentro del usuario y target donde se deposita en dirver response
            @Mapping(source = "lastName", target = "lastName") //si se llaman igual podrian borrarse estas lineas de Mapping, pues se utilizan cuando el nombre cambia
    })*//*

    StateResponse mapEntityToDto(State state);
}
*/
